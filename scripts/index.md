# langs.ts
```
bun run langs.ts
```
Script which merges all languages from every module into one file in public

# translate.ts
```
bun run translate.ts
```
LLM translation which takes en.json, and then translate it using LLM